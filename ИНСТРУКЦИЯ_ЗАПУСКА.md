# 🚀 Полная инструкция по запуску AI Study Agent v2.1

## 🆕 Что нового в версии 2.1

- **Harvard/MIT качество** — улучшенные промпты для генерации курсов уровня топовых университетов
- **Codewars-style практика** — задачи с авто-проверкой через Pyodide (Python в браузере)
- **Кэширование курсов** — экономия 50-70% токенов через Supabase кэш (TTL 1 неделя)
- **RAG поиск** — Wikipedia + Serper для поиска лучших course outlines
- **Формулы KaTeX** — поддержка математических формул в теории
- **Monaco Editor** — профессиональный редактор кода для практики

---

## 📋 Что тебе понадобится

1. **Node.js** (версия 18+) — [скачать](https://nodejs.org/)
2. **Аккаунт Supabase** (бесплатно) — [supabase.com](https://supabase.com)
3. **Аккаунт Groq** (бесплатно) — [console.groq.com](https://console.groq.com)
4. **Опционально:** Serper API (100 бесплатных запросов/месяц) — [serper.dev](https://serper.dev)

---

## 🔧 Шаг 1: Установка Node.js

Если ещё не установлен:
1. Перейди на https://nodejs.org/
2. Скачай LTS версию (рекомендуется)
3. Установи, следуя инструкциям
4. Проверь установку:
```bash
node --version
npm --version
```

---

## 🗄️ Шаг 2: Настройка Supabase (База данных)

### 2.1 Создание проекта
1. Зайди на https://supabase.com
2. Нажми "Start your project" → войди через GitHub/Google
3. Нажми "New Project"
4. Заполни:
   - **Name:** `ai-study-agent`
   - **Database Password:** придумай пароль (ЗАПИШИ ЕГО!)
   - **Region:** выбери ближайший (например, Frankfurt)
5. Нажми "Create new project"
6. Подожди 2-3 минуты пока создаётся

### 2.2 Получение ключей
1. В левом меню нажми ⚙️ **Settings**
2. Выбери **API**
3. Скопируй и сохрани:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** → это `SUPABASE_SERVICE_ROLE_KEY`

### 2.3 Получение DATABASE_URL
1. В Settings выбери **Database**
2. Найди раздел **Connection string**
3. Выбери вкладку **URI**
4. Скопируй строку, она выглядит так:
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```
5. Замени `[YOUR-PASSWORD]` на пароль, который ты создал в шаге 2.1

---

## 🤖 Шаг 3: Настройка Groq (AI)

1. Зайди на 
2. Войди через Google/GitHub
3. В левом меню нажми **API Keys**
4. Нажми **Create API Key**
5. Назови ключ (например, `study-agent`)
6. Скопируй ключ (он показывается только один раз!)

**Бесплатные лимиты Groq:**
- 30 запросов в минуту
- 14,400 запросов в день
- Этого более чем достаточно для обучения!

---

## 📁 Шаг 4: Настройка проекта

### 4.1 Открой терминал в папке проекта
```bash
cd ai-study-agent
```

### 4.2 Создай файл .env
Создай файл `.env` в папке `ai-study-agent` с таким содержимым:

```env
# Supabase (вставь свои значения)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Database (вставь свою строку подключения)
DATABASE_URL="postgresql://postgres:ТВОЙ_ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres"

# Groq AI (вставь свой ключ)
GROQ_API_KEY=gsk_xxxxxxxxxxxxx

# HuggingFace (опционально, fallback)
HUGGINGFACE_API_KEY=hf_xxxxxxxxxxxxx

# Serper (опционально, для RAG поиска)
SERPER_API_KEY=xxxxxxxxxxxxx

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

⚠️ **ВАЖНО:** Замени все значения на свои!

---

## 📦 Шаг 5: Установка зависимостей

```bash
npm install
```

Подожди пока установятся все пакеты (1-3 минуты).

---

## 🗃️ Шаг 6: Настройка базы данных

### 6.1 Генерация Prisma клиента
```bash
npm run db:generate
```

### 6.2 Создание таблиц в базе
```bash
npm run db:push
```

### 6.3 Заполнение начальными данными
```bash
npm run db:seed
```

### 6.4 Создание таблицы кэша курсов (в Supabase SQL Editor)
Зайди в Supabase → SQL Editor и выполни:

```sql
CREATE TABLE IF NOT EXISTS course_cache (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  query_hash TEXT UNIQUE NOT NULL,
  query TEXT NOT NULL,
  analysis JSONB NOT NULL,
  structure JSONB NOT NULL,
  modules JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ NOT NULL,
  access_count INTEGER DEFAULT 0,
  last_accessed_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_course_cache_hash ON course_cache(query_hash);
CREATE INDEX IF NOT EXISTS idx_course_cache_expires ON course_cache(expires_at);
```

---

## 🎉 Шаг 7: Запуск!

```bash
npm run dev
```

Открой в браузере: **http://localhost:3000**

---

## 🔐 Шаг 8: Настройка авторизации через Google

### В Google Cloud Console:
1. Зайди на https://console.cloud.google.com
2. Создай новый проект или выбери существующий
3. Перейди в **APIs & Services** → **Credentials**
4. Нажми **Create Credentials** → **OAuth client ID**
5. Выбери **Web application**
6. Добавь в **Authorized redirect URIs**:
   - `https://ТВОЙ_ПРОЕКТ.supabase.co/auth/v1/callback`
   - `http://localhost:3000/api/auth/callback` (для локальной разработки)
7. Скопируй **Client ID** и **Client Secret**

### В Supabase:
1. Перейди в **Authentication** → **Providers**
2. Найди **Google** и включи его
3. Вставь **Client ID** и **Client Secret** от Google
4. Сохрани

### Проверка:
1. Открой http://localhost:3000/login
2. Нажми "Войти через Google"
3. Авторизуйся через свой Google аккаунт
4. Ты будешь перенаправлен на дашборд!

---

## ❓ Частые проблемы

### Ошибка "Cannot find module..."
```bash
npm install
```

### Ошибка подключения к базе данных
- Проверь DATABASE_URL в .env
- Убедись, что пароль правильный
- Проверь, что проект Supabase активен

### Ошибка Groq API
- Проверь GROQ_API_KEY в .env
- Убедись, что ключ скопирован полностью

### Порт 3000 занят
```bash
npm run dev -- -p 3001
```
И открой http://localhost:3001

---

## 📱 Что дальше?

1. **Создай цель** — нажми "Новая цель" на дашборде
2. **Выбери тему** — например, Python или JavaScript
3. **Пройди диагностику** — AI определит твой уровень
4. **Изучай!** — теория, практика, повторение

---

## 🌐 Деплой на Vercel (бесплатный хостинг)

Когда всё работает локально:

1. Залей код на GitHub
2. Зайди на https://vercel.com
3. Импортируй репозиторий
4. Добавь все переменные из .env в настройках проекта
5. Deploy!

---

## 📞 Нужна помощь?

Если что-то не работает — напиши мне, разберёмся!

---

**Удачи в обучении! 🎓**
